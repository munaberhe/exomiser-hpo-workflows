profiles {
  test {
    params {
      // point this at a real file in your repo
      vcf = "data/case01_hcm.vep.vcf.gz"
      assembly = "GRCh38"   // MUST match that VCF (check with: zgrep '^##reference')
      hpo = "HP:0001639,HP:0001671"
      outdir = "outputs/nextflow_hcm"
    }
  }
}
